---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BettingEducation from '../../components/BettingEducation.astro';
import { getLangFromUrl, useTranslations } from '../../lib/i18n';
const t = useTranslations(getLangFromUrl(Astro.url));
---
<BaseLayout
  title={`${t('edu.title')} — cuotas, margen, cuotas justas, EV y ROI | Fight Prophet`}
  description={t('edu.intro')}
>
  <BettingEducation />
</BaseLayout>
